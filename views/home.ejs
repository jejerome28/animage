<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Welcome to AniMage</h1>
    <form action="/" method="get">
        <label for="search">Search: </label>
        <input type="search" name="search" id="search">
        <button type="submit">Go</button>
    </form>
    
    <div class="homepage">
        <div class="top_anime">
            <h1>Top Airing Anime</h1>
            <% for( let anime of animes ) { %>
                <ul>
                    <img src="<%= anime.images.jpg.image_url %> " alt="<%= anime.title %> picture ">
                    <li><%= anime.title %> </li>
                </ul>
            <% } %>
        </div>

        <div class="season_anime">
            <h1>Top Season Anime</h1>
            <% for( let season of seasonAnime ) { %>
                <ul>
                    <img src="<%= season.images.jpg.image_url %> " alt="<%= season.title %> picture ">
                    <li><%= season.title %> </li>
                </ul>
            <% } %>
        </div>
        
    </div>
</body>
</html>